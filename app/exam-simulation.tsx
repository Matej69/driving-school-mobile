import React from 'react'
import { Text, View } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const ExamSimulation = () => {
  return (
    <SafeAreaView>
      <Text>ExamSimulation</Text>
    </SafeAreaView>
  )
}

export default ExamSimulation